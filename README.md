# Senior-Capstone

## Project can be built with the following commands:
mkdir build

cd build

cmake ..

cmake --build .

## How to run the program:
On mac it can run with **./Capstone**

On windows it can be run with **Capstone.exe**
